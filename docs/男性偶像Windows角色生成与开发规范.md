# 男性偶像 Windows 角色生成与开发规范

> 面向 AI 开发者的强制执行契约。男性版不是另一套技术方案，而是“小曦薇 v3.0.6”固定运行时上的男性人物与动作视觉版本。除人物身份、造型、动作编舞、角色文案、项目身份字符串和该人物公开动态来源外，其他内容全部保持不变。

## 0. 执行指令（最高优先级）

执行本规范的 AI 必须先确认：

```text
我将从 super-idol-hub/xiaoxiwei 的 v3.0.6 开始，
固定基线提交 e9b77f9bb106c8e83c3977e853b7f9eba9e6c1bc。
我不会为男性角色另选框架，不会从空项目重写，不会删掉哭、笑、卖萌、飞吻、睡眠或其他能力。
我只替换人物、造型、动作视觉、角色文案、项目身份字符串和公开动态来源。
我会交付与小曦薇相同形态的单文件 EXE、皮肤包、QA 证据和 SHA-256，
而不是概念图、伪代码、SOP 或未完成的工程骨架。
```

必须完整读取本文件和 [`../contracts/xiaoxiwei-v3.0.6.contract.json`](../contracts/xiaoxiwei-v3.0.6.contract.json)。契约定义不可变行为和验收边界，固定提交定义唯一实现基线。两者出现冲突、缺项或无法验证时，必须停止并报告 `contract-source-conflict`；不得以“源码优先”为理由扩大白名单、改变协议或删减验收。

本规范中的“必须”“不得”“仅允许”是发布门禁。AI 不能因为“男性角色通常不会这样”“换技术更方便”或“动作太难”而改变协议、删除功能或擅自降级。

## 1. 权威基线与复制方式

唯一权威模板：

- 仓库：`https://github.com/super-idol-hub/xiaoxiwei`
- Tag：`v3.0.6`
- Commit：`e9b77f9bb106c8e83c3977e853b7f9eba9e6c1bc`
- 产品形态：C# WinForms 分层透明窗口、单文件 Windows EXE、同级可选 `skins` 文件夹

新男性角色项目必须从该提交 clone 或 fork，并在 Git 历史中保留基线。不得只参考截图重新做一个桌宠，不得复制后创建无关联的新历史，也不得把另一个项目改名后声称“按规范完成”。编码前必须保存：

1. 模板提交号；
2. 新项目显示名、ASCII slug、EXE 名、仓库名；
3. 白名单变更表；
4. 未改动协议声明；
5. 人物素材的私有来源登记位置。

基线差异门禁：

```powershell
git rev-parse HEAD
git show --no-patch --format=%H v3.0.6
git diff --name-status e9b77f9bb106c8e83c3977e853b7f9eba9e6c1bc..HEAD
git diff --stat e9b77f9bb106c8e83c3977e853b7f9eba9e6c1bc..HEAD
```

开工时前两条必须得到固定提交；交付时后两条和 `baseline-proof.json` 必须把每个变化文件映射到白名单。路径按机器契约的大小写敏感、仓库相对完整 glob 规则匹配；每个变化路径必须恰好命中一条规则，零命中或多命中都失败。`XiaoXiWeiPet.cs` 只准改程序集／产品身份、命名空间／资源／Mutex／日志名、动作显示文案、受信任公开页身份和内置皮肤元数据；不得改协议常量、算法、状态机和输入优先级。构建脚本只准改 slug、路径、文件名、资源名、图标、版本、官方皮肤列表和发行名。复制后重写或以“等价实现”替换冻结文件，直接失败。

所有跟踪源码改动必须写入 `identity-replacements.json`。每项记录文件、类型、基线值、替换值和理由；反向应用全部声明替换后，文件 SHA-256 必须与固定提交中的对应 blob 相同。任何未声明 diff hunk、控制流／运算符／方法算法改动、协议数字改动或冻结文件哈希不一致都直接失败。人物关键帧、`.mtn` 和 QA 图属于生成产物或忽略的工作素材，不能作为改运行时代码的理由。

通用 `hatch-pet` 的 `8×11` 原型图集只可用于早期身份或方向实验，不能替代 24 行 Windows 成品，也不能直接放大作为 4K 关键帧。

## 2. 默认禁止与允许改动白名单

### 2.1 仅允许改变

| 变化域 | 允许修改 | 仍然必须保持 |
|---|---|---|
| 人物身份 | 脸、年龄表现、真实体型、发型、肤色、身份锚点 | 同一皮肤 176 帧始终同一人；不能某行突然增肌、变肩宽或变脸 |
| 主造型 | 用户批准的西装、舞台装、日常装、古装、鞋、发饰和常驻道具 | `528×808`、透明背景、统一比例、脚底和视口 |
| 附加皮肤 | 角色或活动造型、皮肤 ID、名称、r15 专属动作视觉 | API v1、完整 176 帧、相同行协议和同一运行时 |
| 动作视觉 | 所有 24 行都要替换成当前人物，并只在固定槽语义内调整手势路径、动作幅度和表情；r12–r15、r18–r21 还可角色化编舞和菜单文案 | 行号、帧数、时长、触发类型、随机权重、视线机制、兼容位和持久状态边界 |
| 产品身份 | 显示名、ASCII slug、命名空间、资源名、Mutex、日志名、图标、EXE/ZIP/仓库名 | 单实例、单文件 EXE、目录、构建参数和 QA 模式 |
| 社交提醒身份 | 该 idol 的公开 URL、提示文字和受信任 host 白名单 | 成功后随机 9–11 分钟；失败按 5／10／20／40／80／120 分钟退避并附加 0–90 秒抖动；首次只建基线、无 Cookie、断网静默退避；不得新增登录、浏览器自动化或抓取框架 |
| 版本 | 新项目版本号与 Release tag | Assembly、README、CHANGELOG、Release Notes 完全一致 |

开发者默认固定为 `Anbunengsi`，Company 固定留空；只有项目负责人明确要求时才能改变。

社交提醒只准替换 URL、提示文字和受信任 host 白名单；不得新增浏览器自动化、登录、Cookie、Token、抓取框架、常驻服务、网络依赖、轮询策略或失败弹窗。若目标来源不能匿名公开访问，应禁用该来源并静默降级，不能改变桌面角色主功能。

### 2.2 一律禁止改变

- C# 5、经典 .NET Framework、WinForms、`System.Drawing` 和 `UpdateLayeredWindow`；
- 24 行、176 张归档帧、`528×808` RGBA、`132×202` 逻辑尺寸；
- 行号、归档／运行帧数、时长数组、随机权重、输入优先级和持久动作分段；
- `17×25`、`XWM1` 运动网格、单人物 owner、至少 24 显示阶段；
- 皮肤 API v1、拖拽、双击、看鼠标、手机、睡眠、换肤、托盘、缩放、置顶、暂停和退出；
- 移动行关闭、自动漫游关闭、`--self-test`、`--qa-window`、构建／打包／哈希流程。
- 本规范的机器契约、schema、帧／时长数组、状态机常量和验收脚本；不得通过修改契约来让不兼容实现通过。

不得改成 Electron、网页、WebView、Python 常驻进程、Unity、Godot、Live2D、WPF、.NET 6+、GIF 或视频播放器。不得只交动作图、演示视频或“以后可以导出 EXE”的项目。

## 3. 冻结的技术实现

### 3.1 编译与运行时

- 编译器：`C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe`
- 参数：`/target:winexe /optimize+ /debug- /platform:anycpu /langversion:5 /codepage:65001`
- 引用：`System.dll`、`System.Core.dll`、`System.Drawing.dll`、`System.IO.Compression.dll`、`System.Windows.Forms.dll`
- 入口：`[STAThread]`
- Manifest：`asInvoker`、`uiAccess=false`、Common Controls v6、PerMonitorV2/PerMonitor DPI aware
- 内置 `frames-4k.zip`、图标、manifest 编译进单文件 EXE
- 正常模式单实例，QA 模式独立 Mutex
- 命令行：`--self-test [report.json] [preview.png]`、`--qa-window [milliseconds]`

Python、NumPy、Pillow、OpenCV 只用于构建；发布 EXE 不能依赖它们、Codex、浏览器或同目录应用 DLL。

### 3.2 窗口与桌面行为

- 无边框、不进入任务栏、默认置顶、右下角启动；
- 使用 `WS_EX_LAYERED | WS_EX_TOOLWINDOW` 和 `UpdateLayeredWindow`；
- 默认 225%，可在 125%–400% 之间按 25% 滚轮步进；
- 右键菜单保留动作、皮肤、微博提醒、置顶、暂停、帮助、退出；
- 托盘图标和托盘双击恢复保留；
- `Ctrl+Q` 退出；
- 产品名替换只允许系统性修改身份字符串，不改变窗口、托盘或输入架构。

## 4. 24 行／176 帧动作协议

r00 归档 7 帧、运行循环 6 帧，c06 为中性兼容姿势。r09/r10 各归档 8 帧却不走定时循环，而是直接按 16 方向选择。AI 不得把“归档数”和“运行数”合并成一张错误的表。

| 行 | 归档 | 运行 | 固定功能语义 | 男性角色默认动作方向 |
|---:|---:|---:|---|---|
| r00 | 7 | 6 | 安静呼吸／眨眼；c06 中性兼容姿势 | 自然站姿、轻呼吸、眨眼、极轻点头；不可一直左右看 |
| r01 | 8 | 8 | 旧版右向移动兼容行，运行时禁用 | 保留符合人物体态的右向步态资源，不参与拖拽 |
| r02 | 8 | 8 | 旧版左向移动兼容行，运行时禁用 | 与 r01 对应；不能挪作新表演 |
| r03 | 4 | 4 | 双击挥手 | 开掌友好挥手，可内敛或外向，但必须读作问候 |
| r04 | 5 | 5 | 跳跃；c00–c02 兼作拖拽受惊／保持 | 垂直小跳或肩手受惊；c02 必须可稳定持有 |
| r05 | 8 | 8 | 疲惫／失败／委屈 | 可垂肩、叹气、低头或平复；不得只因男性身份改成攻击 |
| r06 | 6 | 6 | 等待用户回应 | 询问式抬手、专注前倾或两拍点头；区别于普通待机 |
| r07 | 6 | 6 | 工作 | 眼神扫描、手部小幅操作、专注节拍；不得画成跑步 |
| r08 | 6 | 6 | 检查／审阅 | 轻前倾、扫读、确认点头；必须区别于 r07 |
| r09 | 8 | 直显 | 000、022.5、045、067.5、090、112.5、135、157.5 | 眼睛先行，头颈轻跟随；000 上、090 右必须明确 |
| r10 | 8 | 直显 | 180、202.5、225、247.5、270、292.5、315、337.5 | 与 r09 连续；180 下、270 左必须明确 |
| r11 | 6 | 6 | 疲惫→恢复→皱眉→抬脚→跺脚→鼓气 | 可克制恼火，不得升级为暴力；松手只用 c02–c05 |
| r12 | 8 | 8 | 内置皮肤动作槽 A，固定为“卖萌／俏皮”语义 | 可使用该人物的俏皮签名动作，但正常显示尺寸下必须仍一眼读作卖萌／俏皮；不得替换成敬礼、战斗或普通问候 |
| r13 | 8 | 8 | 内置皮肤动作槽 B，模板为“大笑” | 爽朗或含蓄的大笑；嘴部变化不能改变脸型 |
| r14 | 8 | 8 | 内置皮肤动作槽 C，模板为“哭” | 低头、擦泪、平复；男性同样保留完整情绪能力 |
| r15 | 8 | 8 | 当前皮肤专属动作槽 | 可做招牌问候、整理袖口／外套、舞台定格或角色专属表演 |
| r16 | 8 | 8 | 保留／兼容占位，运行时无触发 | 提供合法 8 帧占位，不改作可触发表演 |
| r17 | 8 | 8 | 保留／兼容占位，运行时无触发 | 提供合法 8 帧占位，不改协议用途 |
| r18 | 8 | 8 | 手势舞／节奏表演槽 | 根据人物真实气质设计清晰的 8 帧手势序列，不强制僵硬或耍帅 |
| r19 | 8 | 8 | 唱歌／舞台表演槽 | 呼吸准备、演唱、收句；不默认替换成说唱或口号 |
| r20 | 8 | 8 | 角色演戏／英气亮相槽 | 可做侠客、喜剧、影视角色或从容亮相，不强制配武器 |
| r21 | 8 | 8 | 飞吻／向镜头亲昵问候／Live Pose；c07 为换肤定格 | c00–c06 必须包含可辨认的飞吻或等价亲昵问候过程，不得退化为纯定格、敬礼或碰拳；c07 必须正面完整且可定格 |
| r22 | 8 | 8 | 持续坐下玩手机 | 坐姿按服装和角色习惯变化，分段和手机连续性不变 |
| r23 | 8 | 8 | 持续侧躺睡觉 | 自然侧卧、入睡、伸展起身；头部位置支持运行时 `z/Z` |

归档计数固定：

```text
7,8,8,4,5,8,6,6,6,8,8,6,8,8,8,8,8,8,8,8,8,8,8,8 = 176
```

所有示例动作均可男女互换；男性版只提供默认起点，不是刻板限制。不得仅因角色为男性删除卖萌、大笑、哭、飞吻、睡眠或脆弱情绪，也不得把每个动作都改成叉腰、耍帅、攻击或健身。

## 5. 不可变交互状态机

### 5.1 拖拽、单击与双击

- 阈值：`max(8px, round(4 × scale))`，默认 225% 时为 9px；
- 超阈值后 r04/c00–c02，拖动期间保持 c02；
- 松手后 r11/c02–c05，共 1700ms；
- 普通单击不能生气；双击必须挥手；捕获丢失不能生气；
- 不重新启用走路、慢跑、快跑和自动漫游。

左键按下的所有权顺序冻结为：正在换肤 → 睡眠／唤醒 → 手机／退出 → 临安持续秋千／退出 → 普通拖拽候选。前四种状态必须消费单击并抑制双击，不能继续落入拖拽判定；只有前四种状态均未持有输入时才能开始普通拖拽候选。

### 5.2 近距离看鼠标

- 进入／退出外半径：220／270 逻辑像素；
- 进入／退出死区：45／34；扇区保持角 14.75°；
- 16 方向每格 22.5°；
- 只在普通 Idle 且无临时动作、拖拽或换肤时启用；
- 鼠标太近或太远时立即退出 r09/r10 并回到 r00；其他动作开始时停止显示 r09/r10，直接播放目标动作，结束后按状态机回 r00；
- 离开范围后只呼吸眨眼，不得持续盯着鼠标；
- r09/r10 与 r00 同尺度、同脚底、同下半身中心，不能看鼠标时缩小或变高。

### 5.3 持续动作

- r22：c00–c03 进入，`3→4→5→4` 循环，单击后 c06–c07 退出；
- r23：c00–c04 入睡，c04 保持，头顶 `z/Z`，单击后 c05–c07 唤醒；
- 对持续状态的单击不能同时变成拖拽或双击；
- 未获编号例外时，新 idol 的 r15 固定为一次性 8 帧动作。只有模板中 `id="linan-princess"` 的既有 r15 使用 `0–2` 进入、`2–5` 循环、`6–7` 点击退出；不得仅凭动作名称让其他皮肤持续。迁移该特例必须把 `LinanSwingContract.SkinId` 映射列为明确例外并同步机器契约。

## 6. 动画平滑与无重影要求

- 皮肤包只有 176 张关键帧，补间不额外打包为 PNG；
- 显示阶段数为 `关键帧数 × ceil(24 / 关键帧数)`：8→24、7→28、6→24、5→25、4→24；
- `7→28` 只是通用计算结果；r00 的运行循环固定为 6 帧→24 阶段，c06 不参与循环；
- 关键帧时长、56 阶段参考节奏、最小 16ms Tick 与机器契约一致；
- 使用 smoothstep、`17×25`、逻辑 `132×202` 的双向 `XWM1` 运动网格；
- 一个中间阶段只能有一个不透明人物 owner，禁止两张完整人物透明交叉淡化；
- 整行动作共享视口、缩放、脚底和主体注册；禁止逐帧自动放大导致体型跳变；
- 官方内置皮肤、官方外置皮肤和正式 Release 必须具备完整 `.mtn`；兼容回退不能作为正式交付。

## 7. 皮肤 API v1 与换肤

目录固定：

```text
<角色名>.exe
skins/
  <skin-id>/
    skin.xml
    frames.zip
```

清单固定：

```xml
<skin apiVersion="1"
      id="skin-id"
      name="皮肤显示名"
      developer="Anbunengsi"
      exclusiveAction="可选专属动作名"
      archive="frames.zip" />
```

规则固定：

- 文件夹名等于 ID；ID 匹配 `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`；
- `apiVersion="1"`、`archive="frames.zip"` 不变；文本字段最长 64 字符；
- ZIP 根目录直接为 `r00/c00.png`；完整 176 帧；
- 同一个 `frames.zip` 还必须包含 `motion/rNN/cNN-rNN-cNN.mtn`；普通官方皮肤恰好 313 个 `XWM1` 网格，临安持续秋千恰好 315 个，并额外包含 `motion/r15/c05-r15-c02.mtn` 与 `motion/r15/c02-r15-c06.mtn`；
- 单条目 ≤32 MiB，ZIP 与解压后总量 ≤128 MiB；
- 冻结运行时会拒绝缺失／重复 PNG、非帧非 motion 文件、不安全路径、尺寸错误、超限条目，以及长度不等于编码长度或前四字节不是 `XWM1` 的 motion；它不会拒绝重复 motion 或名称合法但不在预期集合中的 motion，完整头字段只在请求该网格渲染时解析；
- 正式官方皮肤必须同时具备 `skin.xml`、176 PNG 和完整运动网格，并使用独立发布 QA 把实际有向帧对集合与机器契约精确比较，同时验证完整长度、magic、17×25 网格和 132×202 逻辑头；缺失、重复、意外或无效网格均不得发布。不得把运行时最低校验冒充正式发布门禁；
- 内置皮肤的 r12–r14 可进入动作菜单和随机待机，r15 为内置专属动作；外置皮肤的 r12–r14 与 r16–r17 不可达，只有 `exclusiveAction` 非空时 r15 才可达。仅绘制这些行不能新增运行功能；
- 候选皮肤完整校验／预热后原子切换；失败回退内置皮肤；
- 连续选择只保留最后一次。

换肤固定为 16 个人物 alpha 切片，14+24+8+14=60 阶段、20ms/阶段、约 1200ms：人物伪 3D 旋身 → 新造型由头到脚揭示 → r21/c07 定格 → r00。不得旋转整张矩形窗口或做卡片翻面。

## 8. 男性人物素材与动作流水线

### 阶段 A：私有参考登记

至少需要：

- 同一时期 2–4 张清晰正面／近正面脸；
- 左右三分之四角度各 1–2 张；
- 自然笑、开怀笑、严肃、疲惫、委屈或生气等表情；
- 至少 1 张能看清腿、鞋和真实比例的正面全身；
- 同套造型中实际存在的服装、发型、衣摆、裙摆、裤脚、外套、袖口、鞋、配饰和常驻道具细节。

原图和来源登记放私有目录并加入 `.gitignore`。不得把节目视频、歌曲、歌词、台词、字幕、水印、Cookie、Token 或绝对用户路径提交公开仓库。

### 阶段 B：身份锚点

先生成正面全身中性姿势，锁定脸型、眼距、眉眼、鼻唇、下颌、发色、发型、真实头身比、肩宽、腰线、腿长、服装层次、鞋和常驻配饰。

不得把男性体型自动夸大为宽肩、增肌、长腿或超小头。必须以全身参考为准。用户未批准身份锚点前禁止批量制作 176 帧。

### 阶段 C：角色动作圣经

性别不能作为动作参数。记录具体人物的：

- 中性站姿、惯用手、眼神交流方式；
- 动作能量、手势开合程度、节奏快慢、表情幅度；
- 2–3 个签名动作，例如半笑、整理袖口、两拍点头、固定开掌路径；
- 批准造型实际存在的头发、衣摆、裙摆、裤脚、外套、袖口、配饰和常驻道具的物理约束；不存在的部件不得因性别自动添加。

签名动作可以进入 r00、r03、r06–r08、r12–r15、r18–r21，但不能破坏每行固定功能。不得临时给角色增加武器、花、爱心、麦克风或手机之外的道具，除非身份锚点和用户要求明确包含。

### 阶段 D：逐行动作与 4K 帧

每行先做 4–8 个真正不同的关键姿势联系表并人工批准，再做透明化、整行注册、运动网格和实际尺寸预览。

- 每帧 `528×808` RGBA；
- 人物完整、无背景、无阴影、无字幕、无水印；
- 同一行共享缩放、视口、脚底和主体中心；
- 同脸、同发色、同年龄、同身材、同服装、同配饰；
- 无断手、断脚、断发、断衣摆、断裙摆、断裤脚、断外套、道具消失或双重轮廓；
- 无漂浮爱心、动作线、烟尘、音符或 UI；
- 某行失败只重做该行，不能重写运行时掩盖素材问题。

## 9. 必须复用的构建流水线

不得把脚本名摊平成一条虚假的全局顺序。模板有两条冻结的内部流水线：

1. 内置 EXE：运行 `build.ps1`，由它依次调用 `build_hd_frames.py → validate_action_palette.py → build_motion_fields.py → csc.exe`；
2. 每套官方外置皮肤：运行 `build_skin.ps1`，由它依次调用 `build_hd_frames.py → build_motion_fields.py → pack_external_skin.py`。

两类构建完成后运行 `render_runtime_smoothing_qa.py`、EXE 自检和规定的人工／自动 QA；全部通过后才运行 `package_project.ps1`。不得在外部再次调用 `pack_external_skin.py` 造成重复打包，也不得改变任一内部调用关系。

只允许按 `identity-replacements.json` 精确替换 `xiaoxiwei`、`XiaoXiWei`、中文产品名、皮肤列表、版本、社交身份、发行历史文件名和动作源条带名称；不得更改输入输出协议、安全门禁、控制流、计算或发布要求。

## 10. AI 必须提交的 QA 证据

### 自动门禁

- 24 行、176 张、每张 `528×808` RGBA；
- 每行按归档数提供真正不同且语义完整的关键姿势；归档／运行计数、时长数组、随机权重与契约一致；
- 透明区隐藏 RGB、连通区、内部透明洞、边缘裁切、共享尺度、脚底／主体注册、身份与颜色漂移检查全部通过；
- `.mtn` 完整、`motionFieldLoaded=true`、`ghostFreeSinglePoseOwner=true`；
- 每个循环至少 24 阶段，节奏未改变；
- r09/r10 直接渲染且与 r00 同尺度；
- 走跑和自动漫游保持关闭；
- 拖拽、手机、睡眠、专属动作、换肤和皮肤安全通过；
- EXE `--self-test` 为 `ok=true`、`errors=[]`；
- `--qa-window` 实际启动并正常退出；
- 打包后的 Release EXE 再自检；SHA-256 已生成。

### 人工视觉门禁

必须在 125%、225%、400% 与深浅背景检查：

1. 第一眼能否辨认具体人物；
2. 176 帧是否同脸、同年龄、同发色、同体型、同服装；
3. 是否无尺寸突跳、脚底跳、无意横移、断裂和双人重影；
4. r09/r10 四个基数方向是否明确，鼠标离远是否回普通待机；
5. r22/r23 是否持续且单击退出完整；
6. `z/Z` 是否从睡姿头部上方产生；
7. 换肤是否只旋人物、由头到脚更新、最终 pose 正确；
8. 离线、损坏皮肤和快速连续换肤是否安全；
9. 是否保持头身比、肩宽、胸腰比例、肌肉量、腿长、年龄和体型；男性增肌宽肩只是常见失败示例，不代表其他漂移可以忽略；同时不得仅因男性身份删除情绪或把所有动作做成耍帅攻击。

失败只能修对应动作行、皮肤或身份字符串。不得通过减帧、删功能、关检查、改框架或声明“可选”来过门禁。

## 11. 发布物与目录

```text
<项目名>/
  README.md
  CHANGELOG.md
  RELEASE_NOTES.md
  PROJECT-MANIFEST.json
  identity-replacements.json
  docs/
  source/standalone/xiaoxiwei/  # 冻结的内部实现路径，不随新 idol slug 改名
  skin-pack-template/
  releases/
    最新版/
      <显示名>.exe
      skins/<skin-id>/skin.xml
      skins/<skin-id>/frames.zip
      README.md
      使用说明.md
      皮肤包接口.md
      SHA256SUMS.txt
    历史版本/
    <显示名>-完整包.zip
  qa/
```

新 idol 首版必须应用固定迁移规则 `MIGRATION-001-RELEASE-HISTORY`：只把 `package_project.ps1` 中小曦薇历史文件名字面量清单替换为空清单，不复制任何小曦薇历史 EXE，其他归档、哈希和打包算法不变。从第二版起，替换“最新版”前只归档该 idol 项目真实发布过的上一版，并把实际历史文件名登记在 `identity-replacements.json`。

单独发送 EXE 可以运行内置皮肤；外置皮肤仍需同级 `skins`。不得声称外置皮肤已嵌入 EXE，也不得声称 EXE 静态包含 Windows/.NET Framework 的所有系统组件。

公开 Git 仓库只放源码和文档；EXE、完整 ZIP 和官方外置皮肤通过 GitHub Release 分发。提交前必须做凭据、绝对路径、原始参考、大文件和隐私扫描。

## 12. 完成定义

AI 只有在下列内容全部存在时才能说“已完成”：

1. 基于固定提交的新项目源码；
2. 白名单变更表和未改动协议声明；
3. 用户批准的男性主身份锚点和皮肤；
4. 每套皮肤的 176 帧、完整 `.mtn`、动作联系表和阶段预览；
5. 实际编译成功的单文件 EXE；
6. `--self-test`、`--qa-window`、平滑、拖拽、看鼠标、手机、睡眠、换肤、皮肤安全和离线证据；
7. 最新版、完整 ZIP、皮肤包、使用说明、免责声明和 SHA-256；
8. 干净 Windows 环境双击烟测；
9. README、程序集元数据、版本、CHANGELOG、Release Notes 一致；
10. 获得负责人授权后的 GitHub 仓库与 Release 上传。

规定的最小证据文件包括：`baseline-proof.json`、`identity-replacements.json`、`frame-build-report.json`、`motion-build-report.json`、每套官方皮肤的 `skin-pack-report.json`、`self-test-report.json`、QA 窗口启动／干净退出日志与截图、125%／225%／400% 深浅背景截图、交互证据、`clean-windows-smoke-test.json`、`SHA256SUMS.txt` 和实际打包脚本生成的 `PROJECT-MANIFEST.json`。前述基线、替换、QA 窗口日志和干净 Windows 烟测属于规范新增的发布证据，由执行 AI／独立 QA 在冻结构建完成后按机器契约 schema 生成，不得为了生成它们改写 v3.0.6 运行时。只有文字自述、截图拼贴或演示视频而没有对应 JSON、日志、产物和哈希，不构成通过证据。

缺少任一项只能报告“进行中”或“需要修订”，不能交付“简化版”“概念版”或“核心完成版”。

## 13. AI 汇报和例外格式

每次汇报：

```text
当前模板提交：
本次白名单变更：
本次未改动的不变量：
已生成产物：
已执行验证及结果：
尚未通过的门禁：
下一步：
```

协议冲突：

```text
exceptionId: 待负责人分配
requestedChange:
reason:
affectedInvariant:
compatibilityPlan:
rollbackPlan:
approval: pending
```

AI 不能自行批准例外。

## 14. 权利与免责声明

本规范只定义技术流程，不授予任何人物姓名、肖像、声音、影视角色、服装、品牌、节目、歌曲、舞蹈或照片的权利。

成品必须声明：个人独立开发、粉丝非商业非盈利使用、非官方、与艺人本人／工作室／经纪机构／品牌／平台无授权或合作关系；禁止售卖、收费分发、广告引流、冒充官方、商业宣传和误导性使用，并提供权利方联系和下架渠道。

不得嵌入节目原声、完整歌曲、歌词、台词、字幕、视频、水印、Cookie 或账号凭据。动作只提炼公开气质并重新设计为桌面角色循环，不逐帧复制受保护表演。

---

男性版本只改变人物参考和默认动作设计，不削减任何情绪与互动能力。一个男性角色可以可爱、安静、落泪、飞吻或跳手势舞，也可以英气、喜剧、沉稳或活泼；最终方案由具体人物与用户批准决定，不由性别刻板印象决定。
